# Technical Debt Ledger

This file is the single source of truth for deferred work, known hacks, and
"good enough for now" decisions. It is maintained from build-plan step 00
onward. See `plan/README.md` → "Quality Bar & Anti-Debt Discipline" for the
rules.

## Format

One line per item:

```
step NN | <file>:<area> | <what is the debt> | <FIX NOW | TRIGGER: concrete future event>
```

- `FIX NOW` items **must** be resolved before the next dedicated review
  milestone (`07`, `13`, `25`, `28`).
- `TRIGGER:` items name the exact future event that forces revisiting them.
- At each review, reconcile: every item is either resolved (delete the line)
  or re-justified with a fresh trigger. No item lives forever unchallenged.
- If this file is empty, that is the goal state — say so explicitly in each
  review ("DEBT.md empty: confirmed").

## Active items

step 00 | .cargo/config.toml:rustflags | Dropped `-static-libwinpthread` from the Windows GNU static-link rustflags: this build host's MinGW-w64 GCC 14 uses the win32 thread model, which rejects that flag (it is a posix-thread-model flag) and does not link winpthread at all, so the two remaining flags already yield a self-contained exe (verified via objdump). | TRIGGER: build host switches to a posix-thread-model MinGW (`x86_64-w64-mingw32-gcc -v` reports `--enable-default-msvcrt`/posix threads), at which point re-add static winpthread linking to keep the exe free of `libwinpthread-1.dll`.
step 03 | src/flv_parser.rs:OversizedTag path | On `OversizedTag` the framer clears its buffer and resets to the `PrevTagSize` state, dropping any bytes after the bad tag header. This is not a real resync scan; it merely stops the multi-MiB allocation and hands control back to the caller. | TRIGGER: step 26 (error-handling-and-resync) implements the resync scan — at that point replace the buffer-clear with byte-retention so the scanner can locate the next valid tag boundary.
step 11 | src/rtsp_server.rs:logging | The RTSP server runtime (accept loop, `handle_client`, RTP pump) handles every error path by closing the connection / removing the client but does not yet emit log lines: no module in the RTSP cluster wires the `logging::Logger`, and `RtspServer` has no logger field. Connection/parse/disconnect events are silently handled, not swallowed. | TRIGGER: step 28 (polish-and-hardening) wires structured logging across the server — at that point thread an `Arc<Logger>` (or equivalent) through `RtspServer`/`ConnectionCtx`/`run_pump` and log connection accept/disconnect, parse errors, pump write failures, and the 503/461 boundary responses.
step 11 | src/rtsp_server.rs:session idle timeout | `SESSION_TIMEOUT_SECS` (60) is advertised in SETUP `Session:` headers but never enforced: an idle RTSP TCP connection that never sends TEARDOWN is held open indefinitely (the non-blocking read loop has no idle deadline, and a streaming-but-silent PLAY session must not be killed by a read timeout, so the simple `set_read_timeout` approach is wrong). Max-clients and read-buffer caps are implemented; idle-session reaping is not. | TRIGGER: step 26 (error-handling-and-resync) implements client cleanup — at that point add a session-idle reaper (track last-activity per session, reap sessions idle past `SESSION_TIMEOUT_SECS` that are not in `playing` state) and a RTCP-based keepalive decision for playing sessions.
step 14 | src/camera_listener.rs:plain-TCP ingest (RESOLVED by step 20) | The `CameraListener` accepts a plain-TCP FLV push on `listen_port`. The step-20 interim recon confirmed this **is** the real 7550 production path: the camera sends bare FLV (no uPFLV prefix) over plain TCP after the 7442 AVClient handshake completes. The earlier assumption that 7550 was WSS/TLS was wrong — the camera dials plain TCP on 7550 and pushes `FLV\x01\x07...` directly. `detect_and_strip_prefix` handles both prefixed (step-14 SSH-bypass) and bare-FLV (step-20 production 7550) streams with the same code. The `CameraListener` is therefore both the Linux test ingress and the Windows production 7550 listener. | RESOLVED — no action pending.
step 16 | src/bin/protect_recon.rs + src/protect_recon_impl.rs:recon findings (CAPTURED — step complete) | The step-16 listen-only recon capture tool was run against a real UVC G5 camera (fw 4.73.112, model 0xa591, MAC 28704E11B531, Device-ID 40941af9-a767-5d-662-b57a-deacddd4354d). All open questions are **answered from ground truth** — redalert's second-hand description is no longer the spec. (a) **No stage-2 HTTPS adoption is required**: typing the controller IP in the camera UI makes the camera dial 7442 directly — it never attempted 443. The camera retries 7442 aggressively (every ~1s) while the handshake stays incomplete. Step 19 therefore does **not** need the `/api/1.2/manage` 443 endpoint. (b) **The 7442 handshake is standard RFC 6455 over TLS.** First message is an HTTP/1.1 Upgrade request (518 bytes): `GET /camera/1.0/ws HTTP/1.1` with `Host`, `Origin: http://ws_camera_proto_secure_transfer`, `Upgrade: websocket`, `Connection: close, Upgrade`, `Sec-WebSocket-Key: <base64>`, `Sec-WebSocket-Protocol: secure_transfer`, `Sec-WebSocket-Version: 13`, plus camera-identifying headers `Camera-MAC`, `Camera-IP`, `Camera-Model`, `Camera-Firmware`, `Device-ID`, `Adopted: false`, `x-guid`. The server completes the RFC 6455 handshake (compute `Sec-WebSocket-Accept`) and echoes the `secure_transfer` subprotocol. (c) **The post-upgrade AVClient protocol is JSON-over-binary-WS-frames.** Camera frame 1 (opcode `0x2` binary, 214 bytes): `{"from":"ubnt_avclient","functionName":"ubnt_avclient_timeSync","inResponseTo":0,"messageId":79364096,"payload":{"timeDelta":0},"responseExpected":true,"timestamp":"2026-06-19T15:52:59.817+00:00","to":"UniFiVideo"}`. Frame 2 (opcode `0x9` ping, 6 bytes): `ping-0` — a UniFi keepalive (NOT a generic WS ping; the server must reply with a `pong-N` text frame, not a WS pong control frame). The camera then forcibly closed (os error 10054) because the recon tool replied generically instead of with the UniFi-specific `pong-0` and the `timeSync` response that `responseExpected:true` demands. The framing envelope (binary frame = JSON AVClient message; ping control frame = `ping-<N>` text payload) is the authoritative shape for step 19. | TRIGGER: step 18 (protect-ws-framing) implements the production RFC 6455 layer against the confirmed request shape (path `/camera/1.0/ws`, subprotocol `secure_transfer`, parse the `Camera-*`/`Device-ID`/`x-guid` headers as the adoption context). Step 19 implements the AVClient JSON protocol against the captured envelope: a `from`/`to`/`functionName`/`messageId`/`inResponseTo`/`payload`/`responseExpected`/`timestamp` schema, where the first camera message is `ubnt_avclient_timeSync` (reply required) and keepalives are `ping-<N>` text pings answered with `pong-<N>` text frames. The recon binary is throwaway and is removed (or moved under `tools/`) once step 21 confirms the real path works.
step 16 | Cargo.toml + src/bin/protect_recon.rs + src/protect_recon_impl.rs:schannel zero-crates violation — RESOLVED in step 17 | Step 16's throwaway recon tool added the `schannel` crate (pulling `windows-sys` + `windows-link` transitively) as a Windows-only dependency, violating `PROJECT.md` line 7 / line 120. Step 17 replaced it with the hand-rolled `src/tls_schannel.rs` SSPI module, deleted the `[target.'cfg(windows)'.dependencies]` block from `Cargo.toml`, and regenerated `Cargo.lock` back to just `flvproxy`. The tree is now fully zero-crates on every target. (Entry retained as the resolution record; no action pending.) | TRIGGER: none — resolved.
step 17 | src/tls_schannel.rs:edge cases + write/read hardening | The hand-rolled TLS is validated against exactly one camera (fw 4.73.112) and one .NET `SslStream` client. Edge cases (very large frames, rapid reconnects, partial records at unusual boundaries) are covered by the self-test's 1 B / 64 KiB / 1 MiB round-trips but not exhaustively. Two simplifications remain: (a) `TlsStream::write` sends each encrypted record with a single `stream.write_all` and propagates `WouldBlock`/`TimedOut` as a fatal connection error (the record may be half-sent) rather than buffering+resending like the `schannel` crate — acceptable because the recon/self-test sockets set no write timeout, but production (steps 18–21) may need resumable writes; (b) the streaming `Read` path propagates `WouldBlock`/`TimedOut` from the underlying socket (no internal retry/deadline) so the caller controls cadence, which is correct but means a malformed-record / peer-reset path is not yet hardened. | TRIGGER: step 26 (error-handling-and-resync) hardens the TLS read/write loops against the never-crash guarantees, exercising malformed-record and peer-reset paths and adding resumable write buffering if the production path needs it.
step 17 | src/tls_schannel.rs:PFX private-key persistence + cleanup (🛑 HUMAN ACTION) | `TlsAcceptor::from_pfx` calls `PFXImportCertStore` with flags = 0 (matching step 16's proven `schannel`-crate path), which **persists the PFX's private key to the user's CSP/KSP container** on disk for the process's lifetime. `PKCS12_NO_PERSIST_KEY` (0x8000) was tried and rejected: it yields `SEC_E_NO_CREDENTIALS` (0x8009030E) because the non-persisted in-memory `NCRYPT_KEY_HANDLE` is not resolvable by `AcquireCredentialsHandleA` for a server credential on this Windows config. The persisted key is therefore accepted as the working approach — it is standard behavior for a Windows service identity and produces no UI prompt. **Cleanup:** each run of the proxy leaves a self-signed cert + key in the current user's Personal store. The operator can remove stale entries via `certmgr.msc` → Personal → Certificates → delete the `CN=protect-recon` entry (or `Get-ChildItem Cert:\CurrentUser\My | Where-Object { $_.Subject -eq 'CN=protect-recon' } | Remove-Item`). The production service reuses the same persisted-key approach as its service identity and is not affected by this cleanup. The recon tool that originally created the `protect-recon` cert has been deleted (step 21 complete). | The production proxy uses `flvproxy_cert.pfx` (or a configurable `cert_path`); the `protect-recon` cert is a leftover from the step-16/17 recon testing and can be cleaned up at the operator's convenience.
step 16 | src/bin/protect_recon.rs + src/protect_recon_impl.rs + step-17 self-test scaffolding:throwaway harness (RESOLVED) | The recon capture tool (`protect_recon` binary + impl module) and the step-17 `--selftest` driver + `tools/tls_selftest.ps1` were throwaway validation scaffolding, not production code. The `tls_schannel` module added in step 17 is **not** throwaway — it is the production TLS foundation for steps 18–21. Step 21 confirmed the real-camera no-SSH path works end-to-end; the recon files and Cargo target have been deleted. | RESOLVED — no action pending.
step 18 | src/ws.rs:hand-rolled SHA-1 for WebSocket handshake | RFC 6455 §1.3 requires `Sec-WebSocket-Accept = base64(SHA1(key + GUID))`. Per the project's zero-crates constraint, SHA-1 (FIPS 180-4 / RFC 3174) is implemented by hand in the private `ws::sha1` rather than pulling a crypto crate, and reused only via the public `ws::accept_key`. The implementation is isolated and documented as WS-handshake-only (not a general crypto primitive) so it is not reused for any security-sensitive path — production crypto goes through Windows SChannel (step 17). The alternative (`CryptCreateHash` via SChannel) is Windows-only and would break the Linux-testable invariant, so hand-roll is the deliberate choice. | TRIGGER: step 25 (ONVIF cluster review) re-evaluates the hand-rolled SHA-1 — if a reviewer prefers the SChannel hash path on Windows, `accept_key` gains a `#[cfg(windows)]` SChannel-backed variant while the Linux test path keeps the hand-rolled one.
step 18 | src/ws.rs:decoder mask policy is lenient | RFC 6455 §5.1 mandates that client→server frames be masked and that a server MUST fail the connection on an unmasked client frame. `ws::parse_frame` instead unmasks iff the mask bit is set and accepts unmasked frames otherwise. Strict rejection would break the loopback round-trip tests (the server encoder's own unmasked output is read back) and buys nothing on the production path, where the camera always masks; the server encoder (§5.3) never masks. | TRIGGER: step 25 (ONVIF cluster review) decides whether to add a strict-masked-only mode for the production read path (gated behind a parameter so tests keep the lenient path).
step 19 | src/protect_controller.rs:non-timeSync handler shapes partially confirmed | The step-20 interim recon confirmed the `ubnt_avclient_timeSync` handler (the camera sends ~10 timeSync requests, then sends `hello`), the `ping-0`→`pong-0` keepalive, the `hello` reply (camera accepted our `protocolVersion:67` + features), the `paramAgreement` exchange (camera acked with an `authToken`), and the `ChangeVideoSettings` exchange (camera replied with a 4024-byte full video config including `video1.avSerializer.destinations = ["tcp://192.168.50.100:7550?..."]`). The camera also sent `EventIspSceneStatus`, `EventSmartAudio`, `EventPoorNetwork`, and `EventStreamChanged` events — all handled with the generic ok reply and accepted. The `timeStamp` field name (capital S) is camera-confirmed. The `paramAgreement` payload shape (`enableStatusCodes`/`useHeartbeats`/`heartbeatsTimeoutMs`) is redalert-baseline and worked, but the camera's ack carried only an `authToken` (not a mirror of our payload), so the exact field set the camera cares about is not fully pinned. | TRIGGER: step 21 (protect-human-test) does the full end-to-end validation; at that point confirm whether the `paramAgreement` payload can be simplified and whether any of the event handlers need non-generic replies.
step 19 | src/protect_controller.rs:AvClientSession uses ws::parse_frame/encode_frame directly, not WsConnection | The camera's UniFi keepalive is a WS **Ping** control frame (opcode 0x9) carrying `ping-<N>` that must be answered with a WS **Text** frame `pong-<N>` (not a WS Pong) — step-16 recon ground truth in `DEBT.md`. `ws::WsConnection::read_frame` auto-replies to a Ping with a Pong and swallows the Ping, which would both answer incorrectly and hide the keepalive from the AVClient layer, so `AvClientSession` uses the lower-level `pub` `ws::parse_frame`/`ws::encode_frame` instead. This is a deliberate deviation from plan 19's literal "owning a `WsConnection`". | TRIGGER: step 25 (ONVIF cluster review) decides whether `WsConnection` should grow a "surface Pings / custom-pong" mode so the 7442 and 7550 paths both use the high-level connection type; if so, switch `AvClientSession` to it and remove this entry.
step 19 | src/protect_controller.rs:hand-rolled JSON subset | A minimal JSON parser/emitter is vendored privately in this module, covering only the shapes the AVClient protocol uses (objects, arrays, strings, integers/floats, bools, null). Per the project's zero-crates constraint, no `serde_json`. Per plan 19's debt notes. | TRIGGER: step 25 (ONVIF cluster review) decides whether to promote it to a shared `src/json.rs` if step 22 (ONVIF SOAP) also needs JSON-ish parsing; if not, it stays private here.
step 19 | src/protect_controller.rs:logging not wired | Like the RTSP cluster (step 11 DEBT), the AVClient session handles malformed-JSON and unknown-`functionName` events by skipping silently — no `logging::Logger` is threaded through `AvClientSession`. Events are handled (not swallowed) but not emitted. | TRIGGER: step 28 (polish-and-hardening) threads the logger through the Protect controller alongside the RTSP server.
step 20 | src/camera_listener.rs:7550 transport confirmed (RESOLVED) | The step-20 interim recon (sub-steps 1–3) confirmed the real 7550 transport against a live UVC G5 camera: **plain TCP, bare FLV** — no TLS, no WebSocket, no uPFLV prefix. The camera sends `FLV\x01\x07\x00\x00\x00\x09` (the standard FLV header) directly over a raw TCP socket. `detect_and_strip_prefix` is a no-op when the stream starts with `FLV` instead of the uPFLV prefix, so the existing `PlainTcpSource` + `run_connection` pipeline handles both the step-14 SSH-bypass path (uPFLV prefix) and the step-20 production 7550 path (bare FLV) with the same code. No separate WSS listener, TLS acceptor, or WebSocket framing is needed on 7550. The `WssUpflvSource` and `wss::WssCameraListener` built earlier in step 20 on the assumption that 7550 was WSS have been removed. There is no controller→camera downlink channel on 7550 — the `ChangeVideoSettings` reply on 7442 is the only stream configuration; the camera pushes FLV bytes unidirectionally over 7550. | RESOLVED — no action pending.
step 20 | tests/common/mod.rs:shared test-helper module + #![allow(dead_code)] | Step 20 introduced `tests/common/mod.rs` as the canonical home for the FLV/AMF/AVC synthetic-stream builders, used by `tests/ws_upflv.rs`. The pre-existing test files (`tests/amf.rs`, `tests/camera_pipeline.rs`, `tests/flv_tag_sm.rs`, `tests/wiring.rs`) still carry their own duplicated copies of the same helpers (pre-existing debt, not introduced here). `common/mod.rs` carries `#![allow(dead_code)]` because it is the standard Rust shared-test-helper idiom: each test file uses a different subset of the canonical builder set (e.g. `ws_upflv.rs` uses the standard-path builders but not `ext_seq_header_body`/`ext_nalu_body`), so some helpers are unused by any one file. | TRIGGER: step 25 (ONVIF cluster review) or step 28 (polish) migrates the four pre-existing test files onto `tests/common/mod.rs`, deletes their duplicated helpers, and removes the `#![allow(dead_code)]` once every helper is referenced by at least one test.
step 21 | src/protect_listener.rs + src/protect_recon_impl.rs:recon tool deleted (RESOLVED) | The step-21 human test passed end-to-end: camera adopts over 7442, pushes extendedFlv over 7550, VLC receives live H.264 video via RTSP. The recon tool (`src/bin/protect_recon.rs`, `src/protect_recon_impl.rs`, `tools/tls_selftest.ps1`, and the `[[bin]] protect_recon` Cargo target) has been deleted per the step-16/17/21 triggers. | RESOLVED — no action pending.
step 21 | src/flv_parser.rs:extendedFlv format reverse-engineered from wire data (RESOLVED) | The step-21 human test against a UVC G5 Bullet (fw 4.73.112) revealed that the camera's `extendedFlv` format differs from both the PROJECT.md description and the redalert reference in three ways: (1) video tags use type byte `0x00` instead of `0x09`; (2) the tag header has swapped field order (`type(1) + ts_low(3) + ts_ext(1) + dsize(3) + sid(3)` instead of `type(1) + dsize(3) + ts_low(3) + ts_ext(1) + sid(3)`); (3) the `prev_tag_size` field is omitted after type-0x00 tags — for heartbeat/telemetry tags (dsize=0) a 5-byte trailer follows, for real video frames (dsize>0) the next tag header starts immediately. The `FlvParser` now handles all three deviations. The 5-byte trailer content (`01 41 A2 44 E0`, appearing as IEEE-754 float ~20.27, possibly FPS) is skipped but not parsed. | RESOLVED — no action pending. If a future camera/firmware sends different trailer content, the `EXTFLV_TRAILER_BYTES` constant and `SkipExtFlvTrailer` state may need revisiting.
step 21 | src/protect_controller.rs:AVClient ack-loop fix | The camera's `paramAgreement` and `ChangeVideoSettings` acks carry `responseExpected: false` + `inResponseTo != 0`. Replying to them (as the generic dispatcher did) caused an infinite loop (~5 acks/sec until session death). Fixed by skipping replies for `responseExpected: false && inResponseTo != 0`. Events (`responseExpected: false`, `inResponseTo: 0`) are still acked. | No action pending — the fix is in `handle_data`.
step 21 | src/rtsp_server.rs:wall-clock RTP timestamps | The camera's 7442 session cycles every ~10s, causing the FLV stream to restart with timestamps reset to 0. The RTP pump now uses wall-clock elapsed time since PLAY as the RTP timestamp, ensuring monotonically increasing values that survive stream restarts. | No action pending — the fix is in `run_pump`.
step 21 | src/rtsp_server.rs:Windows WSAEWOULDBLOCK write retry | When bursting a 1.2 MB keyframe as ~850 FU-A fragments over TCP-interleaved RTP, Windows returns `WSAEWOULDBLOCK` (os error 10035) when the TCP send buffer fills. `write_all` treats this as fatal; `write_all_retry` retries on `WouldBlock`/`TimedOut` with a 1ms sleep. | No action pending — the fix is in `write_all_retry`.
step 22 | src/onvif_server.rs:logging not wired | The ONVIF HTTP server (`OnvifServer` accept loop, `handle_connection`) handles every error path by closing the connection but does not yet emit log lines: no `Arc<Logger>` is threaded through `OnvifServer`/`handle_connection`, mirroring the RTSP cluster (step 11) and Protect controller (step 19). SOAP request/disconnect events are handled (not swallowed) but not emitted. | TRIGGER: step 28 (polish-and-hardening) wires structured logging across the ONVIF server alongside the RTSP server and Protect controller — at that point thread an `Arc<Logger>` through `OnvifServer` and log accept, request action, parse failure, and disconnect events.
step 22 | src/onvif_server.rs:default firmware/serial not learned from camera | `OnvifConfig::defaults_for` advertises a static firmware (`4.73.112`) and serial (`000000000000`) in `GetDeviceInformation`. The proxy does not yet learn the real camera firmware/serial from the 7442 AVClient `paramAgreement`/`hello` exchange; an NVR that keys device identity on the serial sees a placeholder. | TRIGGER: step 25 (ONVIF cluster review) or step 28 (polish) decides whether to surface the real serial/firmware from the Protect controller session into `OnvifConfig` (likely via a shared `Arc<Mutex<CameraIdentity>>` populated by the 7442 handler and read by `OnvifServer`); if not, keep the static defaults and document them as intentional.
